# 棚卸×ステラ突き合わせによる盗難検知機能 検討メモ (2026-07-31)

## 背景・目的

棚卸表の「消費量」「デイリーカウント」は、いずれもスタッフ(パートナー)の自己申告データ同士の突き合わせであり、盗難があっても双方が同じように記録され続ければ検知できない。stera smart one(決済端末)の実売上数量は支払いを伴う第三のデータ源であり、改ざんしにくいためより強い盗難シグナルになる。

対象は「消費量(棚卸ベース)が実売上数量より一定以上多い」＝支払いを伴わない減少＝盗難/ロス疑い、というロジック。

## 対象範囲

**「販売品類」(スナック・アイス等、都度課金の単品購入商品)のみ**を対象とする。

- ドリンク類(アペックス/トーヨー/CS3、会費でカバーされる部分)は対象外。会員がドリンクを飲む際はQRをかざすが、これはe-MOSS側で管理されておりステラの取引としては記録されないため、正当な会員消費が「支払いを伴わない減少」として誤検知されてしまう
- 東海限定商品(ロッテ トッポ、じゃがりこ等)はステラ管理外の別運用(店頭現金や別POS等)のため対象外
- 天満店限定のポコテインアイス、取り扱い終了済みのGABAも対象外

## データ取得

- **stera公式API(guides.sterasmartone.com)は商品別・店舗別の売上数量取得には使えない**ことを確認済み。Charge(決済)DTOにはSKU/商品コード/数量/店舗IDに相当するフィールドが存在せず、Order/Product/Inventory関連のAPIエンドポイントも提供されていない
- 実売上データは「注文一覧 → 注文詳細CSV」からダウンロードする。日付範囲指定で全店舗分が1回のダウンロードで取得できる(店舗名・商品コード・商品数量の行レベルデータ)
- 突き合わせキーは商品コード(STE0xx)。ただしステラ側の商品マスタ登録漏れで商品コードが空欄の行があるため、フォールバックとして商品ID(prd_xxxxxxxx)も使う
- 商品コード⇔社内PRODUCTS配列の名寄せ表は作成済み(グループ化商品あり。例: STE002は複数のレディーボーデン味を合算した数字しか取れない)
- 取得はPlaywrightでの自動ダウンロードとする(公式APIでは代替不可のため)

## 実行スケジュール

- 毎日 **01:00頃** に、前日分(00:00〜23:59)をまとめて照会する
- ステラの売上反映はほぼ即時のため、日付が変わった直後の時間帯でその日一日分のデータが確定している

## 検知ロジック(二段構え)

日々の差異(消費量 − ステラ数量)は大小に関わらず全て店舗×商品×日付でログに記録する(フィルタして捨てない)。

1. **即時検知**: 単日の差異が **2個以上** → その場で通知
2. **累積検知**: 直近 **7日間の累積差異が3個以上** → 通知

累積側を設ける理由: パートラーの数え間違いはランダム(多く数える日・少なく数える日が混在)なので、積算するとゼロ付近で相殺される。一方、盗難(例: 1日1個ずつ)は一方向に減り続けるため、積算すると数日で閾値を超えて検知できる。単日の閾値だけでは「1日1個」型の継続的な盗難を見逃してしまうため、この二段構えが必須。

閾値(単日2個・7日3個)は初期値であり、実運用しながら調整する前提。

## 記録・表示場所

- **パートナー向けWebポータル(index.html)には一切表示しない**。スタッフが見える場所に出すと、確認自体が難しくなるため
- 既存のバックエンドスプレッドシート(パートナーはアクセス権を持たない)の「棚卸表」タブに列を追加する形で記録する。この表には既に「デイリーカウント」「差異(消費量-デイリーカウント)」「ステラ売上」「原価率」列があり、ステラ実売上を扱う土台は既にある
- 管理者が後から「カウントミスと判明／要調査／継続監視」等のステータスを書き込める確認欄も設ける(既存の異常メモ欄と同じ発想)

## 通知

- **管理者専用の新規LINE WORKSチャンネル**を作成して通知する(既存のエリア別チャンネルとは別)
- 通知文言は「盗難」と断定せず、中立的な表現にする(例:「〇〇店・△△の在庫差異を検知しました（要確認）」)。パートラーの数え間違いの可能性もあるため、断定的な表現は避ける

## 未確定・保留事項

- 管理者専用LINE WORKSチャンネルのID(グループ作成・Bot招待待ち)
- バックエンドスプレッドシートの「棚卸表」タブへの具体的な列追加位置の最終確認
- 検知閾値(単日2個・累積7日3個)は運用しながら調整

## 関連する既存実装(参考)

- `computeInventoryAnomaly()` (index.html): 既存の消費量異常検知(前月比増加・マイナス消費量)。今回の機能も「断定せず理由入力を促す」という同じ設計思想を踏襲する
- `saveInventorySnapshot()` (gas_backend.gs): 棚卸データはstore_id+period_labelで全削除→再書き込みする方式(今回の日次ログとは別物、上書き注意)
- `sendLineWorksNotification(message, channelIdOverride)` (gas_backend.gs): チャンネルIDは`PropertiesService.getScriptProperties()`にLW_CHANNEL_ID_*として保存する既存パターンを踏襲する
