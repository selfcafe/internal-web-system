# 棚卸×ステラ突き合わせによる盗難検知機能 検討メモ (2026-07-31)

## ✅実装済み(2026-07-31、コミット済み・要clasp本番デプロイ)

以下の設計に基づき実装完了。GitHubへのpush済みだが、GAS側は[[project_clasp_gas_deploy]]の手順で
Apps Scriptエディタへの本番デプロイが別途必要(git pushだけでは本番に反映されない)。

- `stera_daily_sales`シート(蓄積型、新設)・`importSteraDailySales(dateStr, csvText)`
- ①表示: `getChecksheetStockChecks(storeId)` + index.htmlの`renderChecksheetTodayForm`
- ①通知: `checkChecksheetStockMismatch(storeId, product)`(`saveChecksheetData`の保存フローとは
  完全に独立、書き込みロックを取らない読み取り専用アクション)。クライアント側は3秒デバウンス
  (`_scheduleStockMismatchCheck`、既存の`_checksheetSyncChain`とは別のタイマー)
- ②月次バックストップ: `buildStockCheckMonthly(storeId, periodLabel)`(店舗タブU列以降に追加)
- `admin-guide.html`に機能説明・店舗間移動の運用手順を追記
- `scripts/import_stera_daily_sales.py`(Playwright、要ログイン情報での実地セレクタ確認・未検証)

## 背景・目的

棚卸表の「消費量」「デイリーカウント」は、いずれもスタッフ(パートナー)の自己申告データ同士の突き合わせであり、盗難があっても双方が同じように記録され続ければ検知できない。stera smart one(決済端末)の実売上数量は支払いを伴う第三のデータ源であり、改ざんしにくいためより強い盗難シグナルになる。

対象は「消費量(棚卸ベース)が実売上数量より一定以上多い」＝支払いを伴わない減少＝盗難/ロス疑い、というロジック。

## 対象範囲

**「販売品類」(スナック・アイス等、都度課金の単品購入商品)のみ**を対象とする。

- ドリンク類(アペックス/トーヨー/CS3、会費でカバーされる部分)は対象外。会員がドリンクを飲む際もQRをかざすが、これはe-MOSS側で管理されておりステラの取引としては記録されないため、正当な会員消費が「支払いを伴わない減少」として誤検知されてしまう
- 東海限定商品(ロッテ トッポ、じゃがりこ等)はステラ管理外の別運用(店頭現金や別POS等)のため対象外
- 天満店限定のポコテインアイス、取り扱い終了済みのGABAも対象外

## ⚠️前提が変わった重要な制約(2026-07-31、設計途中で発覚)

当初「毎日01:00に前日分を照会→単日/7日間累積で閾値判定」という設計を検討していたが、**日次で意味のある消費量データがそもそも存在しない**ことが判明し、方針を転換した。

- お菓子類：チェックシートの「補充数量」は毎日入力されるわけではない(例: 10個補充して10日かけて消費される場合、補充しない9日間はデータが無い=ゼロではなく未記録)。補充数量は消費量の日次代理指標として使えない
- アイス：現場では**納品時の数量と月末の数量しか記録しておらず、日次データが構造的に存在しない**
- 日次比較には「前日末在庫＋当日補充－当日末在庫＝当日消費量」の式が成り立つ必要があるが、在庫の実数を毎日測っていない以上この式は成立しない

→ **日次/週次の閾値判定ロジック(単日2個以上・7日間累積3個以上等)は撤回。** 以下の2段構成に置き換える。

## 最終方針: 2段構成

### ① パートナー向けの気づきの仕組み + 入力時の自動突き合わせ通知(早期発見・抑止力、抜け漏れは②で補完)

チェックシートのデイリーカウント入力欄(補充数量を入れる場所)の近くに、**「前回入力(◯/◯)からの実売上：◯個」**を表示する。

- 「前回チェックシートにその商品の入力をした日」から「今日」までの累積ステラ売上**数量**(個数。金額ではない)を表示。補充が5日おき・10日おきでも、ギャップの長さに関わらず同じ計算式で対応できる(前回入力日を起点にするため)
- 差異・「盗難確認要」のようなラベルは付けず、**素の数字だけ**を見せる。差異を隠しても本人が自分の消費量と引き算すれば同じ結論に至れるため、最初から隠す前提にしない
- **入力の瞬間に自動で突き合わせて通知(2026-07-31追加・実装済み)**：入力から3秒デバウンス後、`saveChecksheetData`とは完全に独立した読み取り専用アクション`checkChecksheetStockMismatch`が「前回入力〜今日」の補充量とステラ売上数量を比較し、**差異が2個以上なら既存Bot「社内ポータル通知」の既定チャンネルへLINE WORKS通知**。表示(本人が気づく)と通知(管理者が能動的に把握)を両方行う。既存の保存フロー(`saveChecksheetData`)には一切変更を加えていない(既存データを壊すリスクを避けるため意図的に分離)
  - 通知文言は断定しない中立表現(「〇〇店・△△で在庫差異(◯個)を検知しました。ご確認ください」)。処分数量は反映していないが、処分は月末の棚卸時にしか発生しない運用のため実質ノイズにならない(下記参照)ので特記不要と判断
  - **グループ化商品はステラのグループ単位で比較する**：レディーボーデン各種・プリングルス各種等、ステラ側が味を区別しない商品は、`STERA_SALES_MAPPING`の`ourProducts`単位でグループ化し、グループ内のどの味が入力されてもグループ全体の「前回チェックポイント」を更新し、グループ合算の補充量で比較する(個別の味ごとに比較すると、味の内訳のブレで誤検知するため)
  - **処分数量は①では無視して良い(2026-07-31確定)**：処分は月末の棚卸完了時にまとめて行う運用で、月中に発生することはない(期限が近づいた商品は他店に移動させる運用で、実際に捨てることはほぼ無い)。よってノイズ源として重要ではない
  - **店舗間の在庫移動は既知の盲点、対応は②の確認欄で人間が処理**：期限が近い商品を他店に移動する運用があり、移動元店舗では「支払いを伴わない減少」に見えて誤検知しうる。ただし頻度が低く、かつ現在は管理されていない別システム上で起きている現象のため、ポータル移行後は減る見込み。専用の記録機能は今回作らず、②の確認欄で「店舗間移動でした」等を人間が記入する運用で対応する
- **既知の限界**：本人がチェックシートを開かない・その商品を触らない期間が続くと、その間の異常には誰も気づかない(表示・通知ともに入力がトリガーのため)。これは②で補う
- **アイスは対象外**(チェックシートに入力欄がそもそも無いため、表示・比較する場所が無い。アイスも対象にするならチェックシート側に新規入力欄が必要——今回はスコープ外、まずお菓子類のみで開始)

### ② 管理者向けの月次バックストップ(出勤頻度に関わらず毎月必ずカバー、パートナーには非表示)

棚卸完了時に、**消費量(棚卸ベース)とステラの月間売上数量(個数)** を店舗タブ上で突き合わせる。①がカバーできない「本人が触らなかった期間の抜け漏れ」を、月1回は必ず全店舗・全対象商品でカバーする最後の安全網。

- 既存の「棚卸店舗タブ」(`buildStoreInventorySheet`が生成する店舗名タブ、例:「渋谷神南」。⚠️詳細は[[feature_inventory_store_display_tab]]参照——文字通り「棚卸表」という名前のシートではない)に列を追加する
- この表には既にP列以降に「ステラ売上」「原価率」ブロックがあり(`buildSalesCategoryCostRatio`)、土台は既にある。今回は**数量**の比較列を追加する(既存は金額ベースの原価率のみ)
- **パートナー向けWebポータル(index.html)には一切表示しない**。店舗タブはパートナーがアクセス権を持たないバックエンドスプレッドシート側なので問題ない
- 管理者が後から「カウントミスと判明／要調査／継続監視」等のステータスを書き込める確認欄も設ける(既存の異常メモ欄と同じ発想)
- 通知(LINE WORKS等)を付けるかは未定——月次なので緊急性は低く、店舗タブを開いた時に見えれば十分という可能性もある。必要なら既存Bot「社内ポータル通知」の既定チャンネルへ(2026-07-31、新規チャンネルは作らずテスト運用の位置付け)

## データ取得(①②共通)

- **stera公式API(guides.sterasmartone.com)は商品別・店舗別の売上数量取得には使えない**ことを確認済み。Charge(決済)DTOにはSKU/商品コード/数量/店舗IDに相当するフィールドが存在せず、Order/Product/Inventory関連のAPIエンドポイントも提供されていない
- 実売上データは「注文一覧 → 注文詳細CSV」からダウンロードする。日付範囲指定で**店舗数に関わらず全店舗分が1回のダウンロードで取得できる**(店舗名・商品コード・商品数量の行レベルデータ)。100店舗規模に増えてもこの部分の操作回数は変わらない
- 突き合わせキーは商品コード(STE0xx)。ただしステラ側の商品マスタ登録漏れで商品コードが空欄の行があるため、フォールバックとして商品ID(prd_xxxxxxxx)も使う(既存の`STERA_SALES_MAPPING`をそのまま再利用できる)
- 取得はPlaywrightでの自動ダウンロードとする(公式APIでは代替不可のため)。`scripts/import_stera_daily_sales.py`として実装済み(要ログイン情報での実地セレクタ確認・未検証)
- ①のため、店舗×商品ごとの**日次**ステラ売上数量は毎晩取り込んで蓄積しておく必要がある(「前回入力日からの累積」をいつでも計算できるようにするため。②は月次集計だけなので、月1回のCSV取込みでも足りる)。この蓄積は`stera_daily_sales`シート(新設)に持つ——既存の「ステラ注文詳細」タブは使い捨て設計([[project_genkaritsu_cost_ratio]]で確定済み)のため流用できない

## スケール対応(100店舗規模を想定した実装上の注意)

- **N+1回避が必須**: `getChecksheetData()`は本来storeId省略で全店舗分を一括取得できる。店舗ごとにループしてこの関数を呼ぶ実装にすると毎回シート全体を読み直してしまうため、**必ず1回だけ全件取得→メモリ上で店舗ごとにグルーピング**する(既存の`buildInventoryRollup`/`_allStoreIds_`と同じ「先に全部読む」パターンに合わせる)。`inventory_log`側も同様
- ステラCSVの取得・集計自体は店舗数に依存しない(1回のCSVで全店舗分)ため問題なし

## 検討して不採用にした案(記録として残す)

- **単日2個以上/7日間累積3個以上の閾値判定+管理者への都度通知**：日次消費量データが存在しないため前提が崩れ撤回(上記「前提が変わった重要な制約」参照)
- **ステラの「理論在庫」機能を使う案**：入庫(納品)を店舗ごとに個別画面で手動/CSV入力する必要があり(APIも全店舗一括の経路も無い)、読み取り側が全店舗一括でできるかも未調査。書き込み・読み取りとも店舗ごとの個別対応になる可能性が高く、既存の月次突き合わせより手間が増えると判断し、詳細調査の前に不採用(2026-07-31)
- **パートナーに見せる情報から差異・盗難確認要フラグを隠す案**：素のステラ売上数を見せる以上、本人が自分の消費量と引き算すれば同じ結論に至れてしまうため、"隠す"ことに実効性が無いと判断。①では最初から隠さない前提に変更
- **抜け漏れ対策のプッシュ通知(N日間チェックシート未入力なら本人/管理者にリマインド)**：不要と判断(2026-07-31)。②の月次バックストップで代替する

## 未確定・保留事項

- **clasp本番デプロイがまだ済んでいない**(git pushのみ、Apps Scriptエディタへの反映が別途必要)
- `scripts/import_stera_daily_sales.py`のセレクタが未検証(ログイン情報を持つ人による実地確認が必要)
- `buildStoreInventorySheet`の自動反映が現状渋谷限定のため、②も渋谷限定でスタート。他店舗展開はこの自動反映自体の全店舗展開が前提になる([[feature_inventory_store_display_tab]]参照)
- アイスを①の対象にするかどうか(チェックシート側に新規入力欄が必要、今回はスコープ外)
- 「棚卸表の商品をフレーバー単位で分ける必要は無いのでは」という商品マスタ構造の見直し案が出たが、影響範囲が大きいため別の機会に検討する方針(2026-07-31、今回の実装には影響しない——比較ロジック側のグループ化で吸収済み)

**解決済み(2026-07-31、まとめ)**：グループ化商品のズレ→ステラのグループ単位で比較して解消。処分数量→月末の棚卸完了時にしか発生しない運用のためノイズにならない。店舗間の在庫移動→低頻度・ポータル移行で減少見込みのため②の確認欄で人間が処理する運用とし、専用機能は作らない。①の通知閾値→差異2個以上。

## 関連する既存実装(参考)

- `buildStoreInventorySheet()` / `buildSalesCategoryCostRatio()` (gas_backend.gs): 店舗タブの生成・ステラ原価率連携の既存実装。詳細は[[feature_inventory_store_display_tab]]メモ参照
- `STERA_SALES_MAPPING`: 商品コード(STE0xx)/商品ID(prd_xxx)⇔社内PRODUCTS配列の名寄せ表。既存のものをそのまま再利用する
- `getChecksheetData(storeId)` / `_checksheetProductColumns()` (index.html/gas_backend.gs): チェックシートの日別入力データ構造。「前回入力日」を求める際に読む対象
- `computeInventoryAnomaly()` (index.html): 既存の消費量異常検知(前月比増加・マイナス消費量)。②も「断定せず理由入力を促す」という同じ設計思想を踏襲する
- `saveInventorySnapshot()` (gas_backend.gs): 棚卸データはstore_id+period_labelで全削除→再書き込みする方式(今回の機能とは別物、上書き注意)
- `sendLineWorksNotification(message, channelIdOverride)` (gas_backend.gs): 使う場合、チャンネルIDは`PropertiesService.getScriptProperties()`にLW_CHANNEL_ID_*として保存する既存パターンを踏襲する
