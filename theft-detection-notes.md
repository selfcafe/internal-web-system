# 棚卸×ステラ突き合わせによる盗難検知機能 検討メモ (2026-07-31)

## 背景・目的

棚卸表の「消費量」「デイリーカウント」は、いずれもスタッフ(パートナー)の自己申告データ同士の突き合わせであり、盗難があっても双方が同じように記録され続ければ検知できない。stera smart one(決済端末)の実売上数量は支払いを伴う第三のデータ源であり、改ざんしにくいためより強い盗難シグナルになる。

対象は「消費量(棚卸ベース)が実売上数量より一定以上多い」＝支払いを伴わない減少＝盗難/ロス疑い、というロジック。

## 対象範囲

**「販売品類」(スナック・アイス等、都度課金の単品購入商品)のみ**を対象とする。

- ドリンク類(アペックス/トーヨー/CS3、会費でカバーされる部分)は対象外。会員がドリンクを飲む際もQRをかざすが、これはe-MOSS側で管理されておりステラの取引としては記録されないため、正当な会員消費が「支払いを伴わない減少」として誤検知されてしまう
- 東海限定商品(ロッテ トッポ、じゃがりこ等)はステラ管理外の別運用(店頭現金や別POS等)のため対象外
- 天満店限定のポコテインアイス、取り扱い終了済みのGABAも対象外

## データ取得

- **stera公式API(guides.sterasmartone.com)は商品別・店舗別の売上数量取得には使えない**ことを確認済み。Charge(決済)DTOにはSKU/商品コード/数量/店舗IDに相当するフィールドが存在せず、Order/Product/Inventory関連のAPIエンドポイントも提供されていない
- 実売上データは「注文一覧 → 注文詳細CSV」からダウンロードする。日付範囲指定で**店舗数に関わらず全店舗分が1回のダウンロードで取得できる**(店舗名・商品コード・商品数量の行レベルデータ)。100店舗規模に増えてもこの部分の操作回数は変わらない
- 突き合わせキーは商品コード(STE0xx)。ただしステラ側の商品マスタ登録漏れで商品コードが空欄の行があるため、フォールバックとして商品ID(prd_xxxxxxxx)も使う(既存の`STERA_SALES_MAPPING`をそのまま再利用できる)
- 取得はPlaywrightでの自動ダウンロードとする(公式APIでは代替不可のため)

## 記録・表示場所(新規シートは作らない)

既存の「棚卸店舗タブ」(`buildStoreInventorySheet`が棚卸完了ボタン押下後に自動生成・更新する店舗名タブ、例:「渋谷神南」。⚠️詳細は[[feature_inventory_store_display_tab]]参照——文字通り「棚卸表」という名前のシートではない)に列を追加する形で記録する。この表には既に「デイリーカウント」「差異(消費量-デイリーカウント)」列、およびP列以降に別ブロックとして「ステラ売上」「原価率」列があり、ステラ実売上を扱う土台は既にある。

**日々の生データを蓄積する専用シートは不要と判断(2026-07-31)。** 累積7日間の計算に必要な生データは、どちらも元システム側が既に履歴を保持している:
- ステラ側: 注文一覧の日付範囲指定で「直近7日分」を毎晩まるごと再照会すればよい(ステラ自身が取引履歴を保持)
- チェックシート側: `checksheet_data`のJSON内に日付キーで補充数量が既に記録されている(`getChecksheetData`が返す`data[日付キー]['prod:商品名']`)

→ 毎晩「直近7日分」を都度再取得・再集計するだけで済み、こちら側で日次生データを溜め込む専用シートは持たない。新たに持つのは月次集計の追加列(既存店舗タブに追加)と、フラグが実際に立った日だけの記録(店舗タブの隅に数行追記する程度)のみ。

- 管理者が後から「カウントミスと判明／要調査／継続監視」等のステータスを書き込める確認欄も設ける(既存の異常メモ欄と同じ発想)
- **パートナー向けWebポータル(index.html)には一切表示しない**。スタッフが見える場所に出すと、確認自体が難しくなるため。店舗タブはパートナーがアクセス権を持たないバックエンドスプレッドシート側なので問題ない

## 実行スケジュール

- 毎日 **01:00頃** に、直近7日分をまとめて再照会・再集計する
- ステラの売上反映はほぼ即時のため、日付が変わった直後の時間帯でも前日分のデータは確定している

## 検知ロジック(二段構え)

1. **即時検知**: 単日の差異(消費量－ステラ数量)が **2個以上** → 通知
2. **累積検知**: 直近 **7日間の累積差異が3個以上** → 通知

累積側を設ける理由: パートナーの数え間違いはランダム(多く数える日・少なく数える日が混在)なので、積算するとゼロ付近で相殺される。一方、盗難(例: 1日1個ずつ)は一方向に減り続けるため、積算すると数日で閾値を超えて検知できる。単日の閾値だけでは「1日1個」型の継続的な盗難を見逃してしまうため、この二段構えが必須。

閾値(単日2個・7日3個)は初期値であり、実運用しながら調整する前提。

## スケール対応(100店舗規模を想定した実装上の注意)

- **N+1回避が必須**: `getChecksheetData()`は本来storeId省略で全店舗分を一括取得できる。店舗ごとにループしてこの関数を呼ぶ実装にすると毎回シート全体を読み直してしまうため、**必ず1回だけ全件取得→メモリ上で店舗ごとにグルーピング**する(既存の`buildInventoryRollup`/`_allStoreIds_`と同じ「先に全部読む」パターンに合わせる)。`inventory_log`側も同様
- ステラCSVの取得・集計自体は店舗数に依存しない(1回のCSVで全店舗分)ため問題なし。GASの実行時間上限(6分)に収まるかは実データ量で一度計測しておく
- **通知はダイジェスト化する**: 店舗数が増えると同じ晩に複数店舗でフラグが立つ可能性が増える。1件ずつLINE WORKS通知すると連投で見づらくなるため、1日1回まとめて「本日の要確認: A店の◯◯、B店の△△…」の形式に digest 化する

## 通知

- **新規チャンネルは作らず、既存Bot「社内ポータル通知」の既定チャンネル(`LW_CHANNEL_ID`、channelIdOverride省略)にそのまま送る**(2026-07-31確定、テスト運用としてひとまずこの形。テスト終了後、別Botに切り替える可能性はある)
- 通知文言は「盗難」と断定せず、中立的な表現にする(例:「〇〇店・△△の在庫差異を検知しました（要確認）」)。パートナーの数え間違いの可能性もあるため、断定的な表現は避ける
- 1日1回のダイジェスト形式(上記スケール対応参照)
- 本実装前に既存の`testLineWorksNotification()`を一度実行し、想定通り届くか確認してから組み込む

## 未確定・保留事項

- 店舗タブへの具体的な列追加位置の最終確認
- 検知閾値(単日2個・累積7日3個)は運用しながら調整
- `buildStoreInventorySheet`の自動反映が現状渋谷限定のため、盗難検知を他店舗にも展開する場合はこの自動反映自体の全店舗展開が前提になる([[feature_inventory_store_display_tab]]参照)

## 関連する既存実装(参考)

- `buildStoreInventorySheet()` / `buildSalesCategoryCostRatio()` (gas_backend.gs): 店舗タブの生成・ステラ原価率連携の既存実装。詳細は[[feature_inventory_store_display_tab]]メモ参照
- `STERA_SALES_MAPPING`: 商品コード(STE0xx)/商品ID(prd_xxx)⇔社内PRODUCTS配列の名寄せ表。既存のものをそのまま再利用する
- `computeInventoryAnomaly()` (index.html): 既存の消費量異常検知(前月比増加・マイナス消費量)。今回の機能も「断定せず理由入力を促す」という同じ設計思想を踏襲する
- `saveInventorySnapshot()` (gas_backend.gs): 棚卸データはstore_id+period_labelで全削除→再書き込みする方式(今回の機能とは別物、上書き注意)
- `sendLineWorksNotification(message, channelIdOverride)` (gas_backend.gs): チャンネルIDは`PropertiesService.getScriptProperties()`にLW_CHANNEL_ID_*として保存する既存パターンを踏襲する
